# Diferencias entre Git Local y Git Remoto 💻☁️

Este proyecto muestra una tabla comparativa y una descripción interactiva sobre las principales diferencias entre Git Local y Git Remoto.

## 🚀 Instalación y Configuración de la Herramienta de Versionamiento (Git)

### Instalación Local
1. **Instalar Git**:  
   Descarga e instala Git desde [git-scm.com](https://git-scm.com/).
2. **Configurar Git**:
    - Configurar el nombre de usuario:
      ```bash
      git config --global user.name "Tu Nombre"
      ```
    - Configurar el correo electrónico:
      ```bash
      git config --global user.email "tuemail@example.com"
      ```

### Instalación Web
1. **Crear una cuenta en GitHub**:
    - Ir a [github.com](https://github.com/) y registrarse.
2. **Crear un nuevo repositorio**:
    - Dale un nombre a tu proyecto.
    - Opcional: inicializarlo con un README (en este proyecto lo creamos manualmente).

## 📂 Archivos del Proyecto
- `index.html` → Contiene la estructura de la página y la tabla comparativa.
- `README.md` → Documento con instrucciones del proyecto.

## 🔧 Comandos Git Principales Utilizados
- `git init` → Inicializa un repositorio Git local.
- `git add .` → Agrega todos los archivos al área de preparación (staging).
- `git commit -m "Primer commit"` → Realiza el primer guardado de los archivos.
- `git branch` → Verifica las ramas existentes.
- `git checkout -b rama-nueva` → Crea y cambia a una nueva rama.
- `git push` → Sube los cambios al repositorio remoto.

## 📈 Mejoras Futuras
- Agregar modo oscuro automático basado en las preferencias del usuario.
- Mejorar la accesibilidad para navegación con teclado.

---

¡Gracias por visitar este proyecto! 🚀  
**Desarrollado por:** Jhon Mejía.
